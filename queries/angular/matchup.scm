; Angular's grammar is an html fork with the same element/start_tag/end_tag
; node names, so matchup's html query works for inline `template:` strings in
; component .ts files (already injected as angular by nvim-treesitter).
; inherits: html
